# 实施任务

## 产品规格与共享契约

- [x] 新建表格导入的 OpenSpec 提案和可观察行为规格。
- [x] 更新 PRD 的数据导入范围及格式边界。
- [x] 明确 HTTP 接口、持久化、失败回滚、查询分派与限制。
- [x] 增加共享文件上传限制常量、预览响应 schema 和类型；对应规格场景：预览并选择工作表。

## API 与存储

- [x] 新增迁移，支持 MySQL/文件数据源并记录本地导入表映射；对应规格场景：创建本地问数资源。
- [x] 修复数据源表重建时的 SQLite 外键提交失败；迁移期间关闭外键执行，在提交前执行 `foreign_key_check`，并确认现存资源关联保留。
- [x] 新增 CSV/TSV、XLSX 与 XLS 解析器、表头规范化、类型推断和限制检查，并识别误标扩展名的旧版 Excel 文件；对应规格场景：预览并选择工作表、文件格式或数据规模无效。
- [x] 新增预览与原子导入用例、原始字节 API 路由和可理解的错误映射；对应规格场景：预览并选择工作表、创建本地问数资源、文件格式或数据规模无效。
- [x] 增加文件来源数据源查询和本地表结构复核；对应规格场景：导入数据表结构检查。
- [x] 增加 SQLite SQL 方言和只读查询适配器；问数按来源分派并只访问当前资源导入表；对应规格场景：使用导入资源进行问数。
- [x] 保持现有 MySQL 浏览、凭据解密与查询行为；对应规格场景：使用导入资源进行问数。

## Web 页面

- [x] 在数据源管理页加入独立的表格导入弹窗，支持文件选择、工作表切换、预览和创建草稿；对应规格场景：预览并选择工作表、创建本地问数资源。
- [x] 创建成功后复用现有回调自动打开资源配置抽屉；错误时保留可重试状态。
- [x] 设置弹窗内部滚动，保持固定视口和页面级滚动规则；对应规格场景：预览并选择工作表。

## 验收与归档

- [ ] 在 API 服务可用时按五个规格场景完成运行时验收，并检查 MySQL 既有数据源迁移后可用。
- [x] 使用 Node 24 执行 `pnpm check:diagnostics` 和 `pnpm check:spec`；未运行测试套件。
- [ ] 将最终稳定行为合并到 `openspec/specs/data-onboarding/spec.md`，归档本变更。
