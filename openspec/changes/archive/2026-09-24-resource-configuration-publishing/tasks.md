# 任务

## 规格与接口

- [x] 确认草稿、已发布、待复核三种状态及状态转换。
- [x] 定义资源列表、详情、保存、发布和结构复核 API 契约。
- [x] 写明字段级发布校验、聚合限制和结构变更处理规则。

## 数据与后端

- [x] 建立可重复执行且可追踪的 SQLite 迁移机制，新增推荐问题、命名指标和结构复核存储。
- [x] 实现资源列表/详情、完整配置保存和字段级校验。
- [x] 实现发布校验与状态转换。
- [x] 实现结构刷新差异、待复核状态和接受新结构后的字段合并。

## 管理端

- [x] 实现资源列表、详情配置页、字段语义编辑器、指标规则、推荐问题编辑器和结构差异展示。
- [x] 将创建草稿后的占位成功页替换为对应资源配置页。
- [x] 补全加载、空、错误、保存中、校验失败和待复核状态。

## 验收

- [x] 确认以公开 HTTP API 作为回归验收接缝，覆盖列表/详情、保存、发布和结构复核。
- [x] 按确认的公开 HTTP API 接缝添加逐场景回归用例，先验证失败再实现通过。
- [x] 使用演示 MySQL 完成资源创建、配置、发布和无变化结构刷新；通过 API 回归用例覆盖字段新增/删除/改型和接受复核。
- [x] 运行 `pnpm quality` 并逐项核对本变更的场景。
